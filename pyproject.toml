[project]
name = "qtbg-fifo-calculator"
version = "0.1.0"
description = "Calculateur de score QTBG FIFO"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

